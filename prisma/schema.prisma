generator client {
  provider = "prisma-client"
  output   = "./generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Seed script location (run with: pnpm prisma db seed)
// Or manually: npx tsx prisma/seed.ts
